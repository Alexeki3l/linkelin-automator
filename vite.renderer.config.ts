import { defineConfig } from "vite";
// import vue from "@vitejs/plugin-vue"; // O reemplázalo con tu framework (React, Svelte, etc.)
// import path from "path";

// export default defineConfig({
//   root: path.resolve(__dirname, "src/renderer"), // Ruta al directorio del renderer
//   build: {
//     outDir: path.resolve(__dirname, "dist/renderer"), // Salida compilada para el renderer
//     rollupOptions: {
//       input: path.resolve(__dirname, "src/renderer/renderer.ts"), // Archivo de entrada
//     },
//   },
//   resolve: {
//     alias: {
//       "@": path.resolve(__dirname, "src/renderer"), // Alias para facilitar importaciones
//     },
//   },
//   plugins: [vue()], // Agrega aquí los plugins necesarios según tu framework
// });

export default defineConfig({});
